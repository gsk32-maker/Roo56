--[================================]--
--  Script By: A_1g                 --
--  Game: BlockSpin Auto-Farm       --
--[================================]--

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local CoreGui = game:GetService("CoreGui")

-- حذف أي واجهة قديمة لنفس السكربت إن وجدت لتجنب التكرار
if CoreGui:FindFirstChild("A_1g_BlockSpin_GUI") then
    CoreGui.A_1g_BlockSpin_GUI:Destroy()
end

-- إنشاء الواجهة الرئيسية (GUI)
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "A_1g_BlockSpin_GUI"
ScreenGui.Parent = CoreGui

local MainFrame = Instance.new("Frame")
MainFrame.Size = UDim2.new(0, 250, 0, 180)
MainFrame.Position = UDim2.new(0.5, -125, 0.4, -90)
MainFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
MainFrame.BorderSizePixel = 0
MainFrame.Parent = ScreenGui

-- إضافة زوايا دائرية للواجهة
local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 8)
UICorner.Parent = MainFrame

-- عنوان السكربت والحقوق
local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, 0, 0, 40)
Title.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
Title.Text = "BlockSpin Auto-Farm"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
Title.Font = Enum.Font.GothamBold
Title.Parent = MainFrame

local Credit = Instance.new("TextLabel")
Credit.Size = UDim2.new(1, 0, 0, 20)
Credit.Position = UDim2.new(0, 0, 0, 40)
Credit.BackgroundTransparency = 1
Credit.Text = "By: A_1g"
Credit.TextColor3 = Color3.fromRGB(0, 170, 255)
Credit.TextSize = 14
Credit.Font = Enum.Font.GothamSemibold
Credit.Parent = MainFrame

-- زر تشغيل الفارم
local FarmButton = Instance.new("TextButton")
FarmButton.Size = UDim2.new(0.8, 0, 0, 45)
FarmButton.Position = UDim2.new(0.1, 0, 0.55, 0)
FarmButton.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
FarmButton.Text = "تشغيل الفارم (OFF)"
FarmButton.TextColor3 = Color3.fromRGB(255, 255, 255)
FarmButton.TextSize = 15
FarmButton.Font = Enum.Font.GothamBold
FarmButton.Parent = MainFrame

local ButtonCorner = Instance.new("UICorner")
ButtonCorner.CornerRadius = UDim.new(0, 6)
ButtonCorner.Parent = FarmButton

-- متغير حالة الفارم
local farming = false

FarmButton.MouseButton1Click:Connect(function()
    farming = not farming
    if farming then
        FarmButton.Text = "تشغيل الفارم (ON)"
        FarmButton.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
        print("[A_1g Script] تم تفعيل الفارم بنجاح!")
        
        -- هنا يمكنك إضافة اللوب (Loop) الخاص بالفارم حسب متطلبات اللعبة
        task.spawn(function()
            while farming do
                -- ضع كود تجميع الفلوس أو التفاعل هنا
                task.wait(1)
            end
        end)
    else
        FarmButton.Text = "تشغيل الفارم (OFF)"
        FarmButton.BackgroundColor3 = Color3.fromRGB(0, 200, 100)
        print("[A_1g Script] تم إيقاف الفارم.")
    end
end)
